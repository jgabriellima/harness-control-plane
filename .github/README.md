# GitHub Actions — harness-control-plane

| Workflow | Trigger | Role |
|---|---|---|
| `desktop-release.yml` | Tags `v*`, manual | Tauri desktop matrix build |

Control plane SSR/E2E CI to be added when harness fixture workspace ships in-repo.
