---
import AppLayout from '../../layouts/AppLayout.astro';

export const prerender = false;

const { id } = Astro.params;

if (!id) {
  return Astro.redirect('/');
}
---

<AppLayout
  title={`Execution ${id}`}
  description="Jambu execution visibility."
/>
